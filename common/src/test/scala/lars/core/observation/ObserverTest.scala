package lars.core.observation

import org.scalatest.FunSuite

class ObserverTest extends FunSuite {
  test("get eldest") (pending)

  test("get eldest with unobservable") (pending)

  test("get eldest with self observable") (pending)
}
