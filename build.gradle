allprojects {
    group="lars"
    version="1-SNAPSHOT"
}

apply plugin: 'scala'

configurations {
    compile
}

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.scala-lang:scala-library:2.11.8'
}
