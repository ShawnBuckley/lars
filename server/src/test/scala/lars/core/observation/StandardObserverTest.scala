package lars.core.observation

import org.scalatest.FunSuite

class StandardObserverTest extends FunSuite {
  test("convert time") (pending)

  test("split observation") (pending)

  test("observe self observable") (pending)

  test("observe orphan") (pending)
}
